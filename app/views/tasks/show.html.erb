<h1><%= Task.model_name.human %>情報 表示</h1>


<table class="table table-striped">
  <tbody>
   <tr>
      <td><%= t Task.human_attribute_name(:is_finish) %></td>

      <td><%= @task.is_finish %></td>

    </tr>
   <tr>
      <td><%= t Task.human_attribute_name(:name) %></td>

      <td><%= @task.name %></td>

    </tr>
  </tbody>
</table>

<%= link_to '編集', edit_task_path(@task), class: "btn btn-primary" %>
<%= link_to '戻る', tasks_path, class: "btn btn-secondary" %>
